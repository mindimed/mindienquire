<?php

class Booking_engine_settingsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('booking_engine_settings')->truncate();

		$booking_engine_settings = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('booking_engine_settings')->insert($booking_engine_settings);
	}

}
