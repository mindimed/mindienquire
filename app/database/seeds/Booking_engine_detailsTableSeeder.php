<?php

class Booking_engine_detailsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('booking_engine_details')->truncate();

		$booking_engine_details = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('booking_engine_details')->insert($booking_engine_details);
	}

}
