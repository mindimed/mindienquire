<?php

class Booking_enginesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('booking_engines')->truncate();

		$booking_engines = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('booking_engines')->insert($booking_engines);
	}

}
